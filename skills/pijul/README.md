# Pijul

Version control with the Pijul patch-based VCS. Pijul stores changes (patches),
not snapshots, enabling commute, conflict-free cherry-picks, and a
mathematically sound merge model.

## Installation

Place in `~/.pi/agent/skills/pijul/` or install as a Pi package.

Requires `pijul` installed and on `$PATH`.
